{
  "name": "@aloy/cloudflare-workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface Bindings",
    "test": "vitest"
  },
  "dependencies": {
    "@hono/valibot-validator": "^0.3.1",
    "hono": "^4.6.12",
    "valibot": "^0.39.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.31",
    "@cloudflare/workers-types": "^4.20241112.0",
    "vitest": "2.0.5",
    "wrangler": "^3.90.0"
  }
}
